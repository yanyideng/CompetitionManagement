<%= form_for @group do |f| %>

  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@group.errors.count, "error") %> prohibited
        this group from being saved:
      </h2>
      <ul>
        <% @group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :输入指导老师 %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.hidden_field:group_id, :value => f.id %>
  </p>

  <p>
    <%= f.submit :提交%>
  </p>

<% end %>
